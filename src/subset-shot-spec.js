'use strict'


/* global describe, it */
const subsetShot = require('.')

describe('subset-shot', () => {
  it('write this test', () => {
    console.assert(subsetShot, 'should export something')
  })
})
